<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="projectPageLoaded">
  <DockLayout stretchLastChild="true">
    <ListView dock="top" items="{{articles}}">
	    <ListView.itemTemplate>
	        <Label horizontalAlignment="stretch" height="50px" text="{{text}}" />
	    </ListView.itemTemplate>
    </ListView>
  </DockLayout>
</Page>
